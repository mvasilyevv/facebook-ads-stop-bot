# Реестр хуков-атомов. Переиспользуются по id из geo.yaml и слотов.
# level: geo | slot | visual | text. Вердикт — ТОЛЬКО с цифрами (иначе unknown).
# См. docs/creatives/_schema.md
#
# ⚠️ ОБНУЛЁН 2026-06-09 (чистый старт). Наполняется ЗАНОВО и ТОЛЬКО из данных (SOP §R6):
#   - анализ найденных топ-креативов гео (PROMPTING.md §1) → hook-кандидаты;
#   - наши FTD по трекеру → вердикт winner/works/dead с цифрами в evidence.
# НЕ переносим хуки из другого гео «по аналогии» и НЕ выдумываем тестовые гипотезы.
# Нет данных → хука нет. Снапшот прошлого реестра (KE-винеры/анти-примеры) — в _archive/;
# нарратив-история — creative_kb.md §2.
hooks: []
