(async () => { if (window.__fbAutoWidthDragRunning) { console.warn('[auto-width-drag] Уже выполняется предыдущий запуск.'); return window.__fbAutoWidthDragLastResult || null; } window.__fbAutoWidthDragRunning = true; const CONFIG = { tolerancePx: 3, maxPasses: 12, settleMs: 180, dragSettleMs: 260, dragSteps: 10, smallScrollPx: 90, debug: true, }; const TARGETS = [ { key: 'toggle', title: 'Выкл./вкл.', surfaceKey: 'toggle', widthPx: 40 }, { key: 'name', title: 'Название объявления', surfaceKey: 'name', widthPx: 194 }, { key: 'delivery', title: 'Статус показа', surfaceKey: 'delivery', widthPx: 110 }, { key: 'budget', title: 'Бюджет', surfaceKey: 'budget', widthPx: 40 }, { key: 'deposits', title: 'Результат', surfaceKey: 'results', widthPx: 137 }, { key: 'reach', title: 'Охват', surfaceKey: 'reach', widthPx: 99 }, { key: 'impressions', title: 'Показы', surfaceKey: 'impressions', widthPx: 113 }, { key: 'cost_per_result', title: 'Цена за результат', surfaceKey: 'cost_per_result', widthPx: 130 }, { key: 'spend', title: 'Сумма затрат', surfaceKey: 'spend', widthPx: 112 }, { key: 'clicks', title: 'Клики', surfaceKey: 'clicks', widthPx: 102 }, { key: 'cpc', title: 'CPC', surfaceKey: 'cpc', widthPx: 93 }, { key: 'leads', title: 'Лиды', surfaceKey: 'actions', textNeedles: ['лид', 'лід', 'lead'], widthPx: 102 }, { key: 'cost_per_lead', title: 'Цена за лид', surfaceKey: 'cost_per_action_type', textNeedles: ['лид', 'лід', 'lead'], widthPx: 105 }, { key: 'registrations', title: 'Завершенные регистрации', surfaceKey: 'actions', textNeedles: ['регистрац', 'реєстрац', 'registration'], widthPx: 88 }, { key: 'cost_per_registration', title: 'Цена за завершенную регистрацию', surfaceKey: 'cost_per_action_type', textNeedles: ['регистрац', 'реєстрац', 'registration'], widthPx: 100 }, { key: 'ctr', title: 'CTR', surfaceKey: 'ctr', widthPx: 91 }, { key: 'campaign_name', title: 'Название кампании', surfaceKey: 'campaign_group_name', textNeedles: ['название кампании'], widthPx: 40 }, { key: 'adset_name', title: 'Название группы объявлений', surfaceKey: 'campaign_name', textNeedles: ['название группы объявлений'], widthPx: 40 }, { key: 'outbound_clicks', title: 'Исходящие клики', surfaceKey: 'outbound_clicks', widthPx: 40 }, { key: 'outbound_ctr', title: 'CTR исходящих кликов', surfaceKey: 'outbound_clicks_ctr', widthPx: 40 }, { key: 'landing_page_views', title: 'Просмотры целевой страницы', surfaceKey: 'actions', textNeedles: ['целев', 'цільов', 'landing page'], widthPx: 40 }, { key: 'cost_per_landing_page_view', title: 'Цена за просмотр целевой страницы', surfaceKey: 'cost_per_action_type', textNeedles: ['целев', 'цільов', 'landing page'], widthPx: 40 }, { key: 'cpm', title: 'CPM', surfaceKey: 'cpm', widthPx: 40 }, { key: 'frequency', title: 'Частота', surfaceKey: 'frequency', widthPx: 40 }, ]; const sleep = (ms) => new Promise((resolve) => setTimeout(resolve, ms)); const normalize = (value) => String(value || '').replace(/\s+/g, ' ').trim().toLowerCase(); const surfaceOf = (node) => { const surface = node.getAttribute('data-surface') || ''; const match = surface.match(/table_column_header:([^/]+)/); return match ? match[1] : ''; }; function targetMatches(target, surfaceKey, text) { const normalizedText = normalize(text); const titleMatches = Boolean(normalizedText) && normalizedText === normalize(target.title); if (target.surfaceKey !== surfaceKey) return titleMatches; if (!target.textNeedles?.length) return true; if (!normalizedText) return true; return titleMatches || target.textNeedles.some((needle) => normalizedText.includes(normalize(needle))); } function findColumnCell(headerNode) { const directCell = headerNode.closest('._4lg0'); if (directCell instanceof HTMLElement) { const rect = directCell.getBoundingClientRect(); if (rect.width > 20 && rect.height > 20) return directCell; } for (let node = headerNode.parentElement; node instanceof HTMLElement; node = node.parentElement) { const rect = node.getBoundingClientRect(); if (rect.width > 20 && rect.height > 20 && (node.style.width || node.classList.contains('_4lg0'))) return node; } return null; } function clippingBounds(node) { let left = 4; let right = window.innerWidth - 4; let top = 4; let bottom = window.innerHeight - 4; for (let parent = node.parentElement; parent; parent = parent.parentElement) { const style = getComputedStyle(parent); const clipsX = ['auto', 'hidden', 'scroll', 'clip'].includes(style.overflowX); const clipsY = ['auto', 'hidden', 'scroll', 'clip'].includes(style.overflowY); if (!clipsX && !clipsY) continue; const rect = parent.getBoundingClientRect(); if (rect.width <= 0 || rect.height <= 0) continue; if (clipsX) { left = Math.max(left, rect.left + 3); right = Math.min(right, rect.right - 3); } if (clipsY) { top = Math.max(top, rect.top + 3); bottom = Math.min(bottom, rect.bottom - 3); } } return { left, right, top, bottom }; } function collectCandidates(processedKeys = new Set()) { const result = []; const seen = new Set(); const headers = Array.from(document.querySelectorAll('[data-surface*="table_column_header:"]')); for (const headerNode of headers) { const surfaceKey = surfaceOf(headerNode); const text = headerNode.textContent || ''; const target = TARGETS.find((item) => targetMatches(item, surfaceKey, text)); if (!target || processedKeys.has(target.key) || seen.has(target.key)) continue; const cell = findColumnCell(headerNode); if (!(cell instanceof HTMLElement)) continue; const separator = cell.querySelector('._4lg9[role="separator"], [role="separator"]'); if (!(separator instanceof HTMLElement)) continue; const cellRect = cell.getBoundingClientRect(); const sepRect = separator.getBoundingClientRect(); const x = sepRect.left + sepRect.width / 2; const y = sepRect.top + sepRect.height / 2; const bounds = clippingBounds(separator); const visible = cellRect.width > 20 && cellRect.height > 20 && sepRect.width > 0 && sepRect.height > 0 && x >= bounds.left && x <= bounds.right && y >= bounds.top && y <= bounds.bottom; if (!visible) continue; seen.add(target.key); result.push({ target, cell, separator, cellRect, sepRect, x, y, bounds }); } return result.sort((a, b) => a.cellRect.left - b.cellRect.left); } function findHorizontalScroller() { const header = document.querySelector('[data-surface*="table_column_header:"]'); const candidates = []; const seen = new Set(); function add(node) { if (!(node instanceof HTMLElement) || seen.has(node)) return; seen.add(node); const maxScrollLeft = node.scrollWidth - node.clientWidth; const rect = node.getBoundingClientRect(); if (maxScrollLeft <= 8 || node.clientWidth < 180 || rect.height < 40) return; const containsHeader = header ? node.contains(header) : false; candidates.push({ node, maxScrollLeft, score: maxScrollLeft + (containsHeader ? 100000 : 0) + Math.round(rect.width + rect.height), }); } const anchors = [ header, document.querySelector('[data-surface*="table_row:"], ._1gda._2djg'), document.querySelector('[role="grid"]'), document.querySelector('[role="table"]'), document.querySelector('[aria-rowcount]'), ].filter(Boolean); for (const anchor of anchors) { for (let node = anchor; node; node = node.parentElement) add(node); } for (const node of document.querySelectorAll('[role="grid"], [role="table"], [aria-rowcount], div')) add(node); candidates.sort((a, b) => b.score - a.score); return candidates[0]?.node || null; } function dispatchMouse(target, type, x, y, buttons) { const event = new MouseEvent(type, { bubbles: true, cancelable: true, view: window, clientX: Math.round(x), clientY: Math.round(y), screenX: Math.round(window.screenX + x), screenY: Math.round(window.screenY + y), button: 0, buttons, }); return target.dispatchEvent(event); } function dispatchPointer(target, type, x, y, buttons) { if (!window.PointerEvent) return true; const event = new PointerEvent(type, { bubbles: true, cancelable: true, composed: true, pointerId: 1, pointerType: 'mouse', isPrimary: true, clientX: Math.round(x), clientY: Math.round(y), screenX: Math.round(window.screenX + x), screenY: Math.round(window.screenY + y), button: 0, buttons, }); return target.dispatchEvent(event); } async function dragSeparator(candidate, delta) { const startX = candidate.x; const startY = candidate.y; const minX = candidate.bounds.left + 8; const maxX = candidate.bounds.right - 8; const endX = Math.max(minX, Math.min(maxX, startX + delta)); const steps = Math.max(4, Math.min(CONFIG.dragSteps, Math.ceil(Math.abs(endX - startX) / 12))); candidate.separator.scrollIntoView({ block: 'nearest', inline: 'nearest' }); await sleep(40); dispatchPointer(candidate.separator, 'pointerover', startX, startY, 0); dispatchMouse(candidate.separator, 'mouseover', startX, startY, 0); dispatchPointer(candidate.separator, 'pointermove', startX, startY, 0); dispatchMouse(candidate.separator, 'mousemove', startX, startY, 0); dispatchPointer(candidate.separator, 'pointerdown', startX, startY, 1); dispatchMouse(candidate.separator, 'mousedown', startX, startY, 1); for (let i = 1; i <= steps; i += 1) { const x = startX + ((endX - startX) * i) / steps; dispatchPointer(document, 'pointermove', x, startY, 1); dispatchMouse(document, 'mousemove', x, startY, 1); await sleep(16); } dispatchPointer(document, 'pointerup', endX, startY, 0); dispatchMouse(document, 'mouseup', endX, startY, 0); dispatchPointer(candidate.separator, 'pointerout', endX, startY, 0); dispatchMouse(candidate.separator, 'mouseout', endX, startY, 0); await sleep(CONFIG.dragSettleMs); return { requestedEndX: Math.round(startX + delta), actualEndX: Math.round(endX), clamped: Math.abs(endX - (startX + delta)) > 2 }; } async function scrollScroller(scroller, direction, small = false) { if (!(scroller instanceof HTMLElement)) return false; const before = scroller.scrollLeft; const step = small ? CONFIG.smallScrollPx : Math.max(160, Math.round(scroller.clientWidth * 0.7)); const max = Math.max(0, scroller.scrollWidth - scroller.clientWidth); scroller.scrollLeft = Math.max(0, Math.min(max, before + direction * step)); scroller.dispatchEvent(new Event('scroll', { bubbles: true })); await sleep(CONFIG.settleMs); return Math.abs(scroller.scrollLeft - before) > 2; } try { const processed = new Set(); const rows = []; const scroller = findHorizontalScroller(); if (scroller) { scroller.scrollLeft = 0; scroller.dispatchEvent(new Event('scroll', { bubbles: true })); await sleep(CONFIG.settleMs); } for (let pass = 1; pass <= CONFIG.maxPasses; pass += 1) { let changedThisPass = false; for (let guard = 0; guard < TARGETS.length + 2; guard += 1) { const candidates = collectCandidates(processed); const candidate = candidates[0]; if (!candidate) break; const target = candidate.target; const before = candidate.cell.getBoundingClientRect().width; const delta = Math.round(target.widthPx - before); let drag = null; let status = 'skip'; if (Math.abs(delta) <= CONFIG.tolerancePx) { processed.add(target.key); status = 'already'; } else { drag = await dragSeparator(candidate, delta); const after = candidate.cell.getBoundingClientRect().width; const matched = Math.abs(target.widthPx - after) <= CONFIG.tolerancePx; if (matched) { processed.add(target.key); status = 'drag-ok'; } else if (drag.clamped && after > target.widthPx + CONFIG.tolerancePx) { await scrollScroller(scroller, -1, false); status = 'needs-left-scroll'; } else if (drag.clamped && after < target.widthPx - CONFIG.tolerancePx) { await scrollScroller(scroller, 1, true); status = 'needs-right-scroll'; } else { processed.add(target.key); status = 'drag-no-width-change'; } changedThisPass = true; } const after = candidate.cell.getBoundingClientRect().width; rows.push({ pass, key: target.key, title: target.title, before: Math.round(before), target: target.widthPx, after: Math.round(after), delta, status, clamped: Boolean(drag?.clamped), }); } if (processed.size >= TARGETS.length) break; const moved = await scrollScroller(scroller, 1, false); if (!moved && !changedThisPass) break; } const missing = TARGETS.filter((target) => !processed.has(target.key)).map((target) => target.title); const result = { ok: missing.length === 0, processed: processed.size, expected: TARGETS.length, missing, rows }; console.table(rows); if (missing.length) console.warn('[auto-width-drag] Не найдены/не обработаны:', missing); else console.info('[auto-width-drag] Все колонки обработаны через separator drag.'); window.__fbAutoWidthDragLastResult = result; return result; } finally { window.__fbAutoWidthDragRunning = false; } })();
