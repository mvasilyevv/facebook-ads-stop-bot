packages:
  - "packages/*"
  - "frontend"
  - "frontend-mini"

allowBuilds:
  esbuild: true
  "@tailwindcss/oxide": true
  # MSW нужен только как test runtime; его install script не является частью
  # сборки и намеренно запрещён.
  msw: false
