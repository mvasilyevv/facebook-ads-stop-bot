#!/usr/bin/env bash
set -Eeuo pipefail
SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd -P)"
PROJECT_DIR="$(cd -- "$SCRIPT_DIR/.." && pwd -P)"
cd -- "$PROJECT_DIR"
export PYTHONDONTWRITEBYTECODE=1
exec python3 -B -m fbctl "$@"
