# Временные файлы Python
__pycache__/
*.pyc
*.egg-info/
.ruff_cache/
.pytest_cache/
.hypothesis/

# Виртуальное окружение и конфигурация
.venv/
.venv-*/
.env
.env.local
**/.env.local

# Системные файлы и кэш редакторов
.DS_Store
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.swp

# Вспомогательные папки ИИ и агентов.
# .claude/agents/ (определения субагентов), .claude/skills/ (симлинки на
# .agents/skills/) и .claude/commands/ (slash-команды) ВЕРСИОНИРУЕМ — это
# проектные инструкции. Локальное (settings.local.json, plans/, worktrees/)
# остаётся в игноре.
.claude/*
!.claude/agents/
!.claude/skills/
!.claude/commands/
.claude-flow/
.omc/
.mcp.json
.playwright-mcp/
.superpowers/
scratch/

# Временные папки, логи и результаты тестов
.tmp
.tmp/
.logs/
logs/
recordings/
*.pid
playwright-report/
test-results/
**/coverage/
npm-debug.log*

# Зависимости и сборка фронтенда (pnpm monorepo)
node_modules/
**/node_modules/
frontend/dist/
frontend-mini/dist/
packages/*/dist/
# Артефакт TS-сборки browser-agent пересобирают CI и Dockerfile перед
# `node dist/index.js`. Не трекаем, чтобы исключить drift src и артефакта.
services/browser-agent/dist/
**/.tanstack/
frontend/storybook-static/
remotion/out/
services/browser-agent/node_modules/

# Локальные базы данных
*.db
*.sqlite
*.sqlite3

# Секреты и конфиденциальные данные
helm/fb-stop-bot/secrets.yaml
data/secrets_backup_*.json

# Локальные runtime-данные: браузерные профили и cookies, медиа Ad Library,
# креативные генераты, MCP-конфиги и бэкапы. Каталог может быть большим и
# содержать секреты, поэтому целиком исключён из Git и production-rsync.
/data/
.am_capture/

# Скриншоты-черновики сессий (syntx/recon/veo) — только корень репо
/*.png
/*.jpg
/*.jpeg

# Билд-кэш TypeScript
*.tsbuildinfo

# Мониторинг: локальный пароль Grafana
deploy/monitoring/.env.monitoring

# Бэкапы .env с секретами — никогда не коммитить
.env.bak*
